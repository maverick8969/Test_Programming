/**
 * This is a placeholder for the main application.
 *
 * For Phase 1 testing, use the test environments:
 * - pio run -e test_00_blink -t upload -t monitor
 * - pio run -e test_01_buttons -t upload -t monitor
 * - pio run -e test_02_encoder -t upload -t monitor
 *
 * The main application will be developed in Phase 7.
 */

// This file is intentionally empty for now.
// Each test has its own source directory specified in platformio.ini
